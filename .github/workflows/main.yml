# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  pull_request:
    paths-ignore:
        - '.gitignore'
        - README.*
        - LICENSE
        - 'etc/news.scm'
        - 'etc/snippets/**'
        - scripts/**
        - .dir-locals.el
        - .guix-authorizations
        - .guix-channel
        - doc/**
        - manifest.scm
        - .envrc

  push:
    # Sequence of patterns matched against refs/tags
    branches: ["main"]
    paths-ignore:
        - '.gitignore'
        - README.*
        - LICENSE
        - 'etc/news.scm'
        - 'etc/snippets/**'
        - scripts/**
        - .dir-locals.el
        - .guix-authorizations
        - .guix-channel
        - doc/**
        - manifest.scm
        - .envrc

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  run-tests:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6
      # Runs a set of commands using the runners shell
      - name: Build sops package
        run: guix build -L $(pwd)/modules sops

      - name: Run unit tests
        run: guix shell blue -- blue check

      - name: Run integration tests
        run: guix shell blue -- blue system-tests
